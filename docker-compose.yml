version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "9017:9017"
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Asia/Taipei
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "9016:9016"
    depends_on:
      - backend
    restart: always
